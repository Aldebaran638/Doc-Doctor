# Doc-Doctor 第二版需求文档

本版本需求聚焦在在现有注释检查能力基础上，新增「基于 Git 的函数内容变更警告」功能，对函数相对于上一次提交的业务逻辑变更进行提示。

---

## 功能一：基于 Git 的函数内容变更警告

### 1. 功能概述

当用户在首页点击「项目检查」按钮，对当前工作区的 `.c` 文件执行全量检查时，如果某个函数的函数体内容相对于当前分支上一条提交（`HEAD~1`）发生了**实质性变更**，则自动为该函数新增一条问题记录，问题类型为「内容变更警告」（`problem_type = 4`）。

该功能的目的是提醒用户：函数实现已经变化，请检查并同步更新 Doxygen 注释（`@brief/@param/@return` 等）。

此功能仅在「项目全量检查」中启用；单文件检查不做 Git 变更分析。

---

### 2. 功能流程

```mermaid
flowchart TD
    A[用户点击项目检查按钮] --> B[读取设置 doc-doctor.enableGitChangeWarning]
    B -->|false/未开启| C[按原有规则执行检查\n不进行 Git 变更分析]
    B -->|true/已开启| D[检测当前工作区是否为 Git 仓库\n且存在至少两条提交]
    D -->|否| E[在输出区提示\n"当前项目未使用 git，已跳过变更检测"]
    D -->|是| F[执行 git diff HEAD~1\n仅获取 .c 文件的变更列表]
    F --> G[对变更的 .c 文件解析函数\n通过文件路径+函数签名匹配 HEAD~1 中对应函数]
    G --> H{函数是否在 HEAD~1 中存在?}
    H -->|否/新函数| I[视为新增函数\n不生成变更警告]
    H -->|是/既有函数| J[比较当前函数体与 HEAD~1 中的函数体]
    J --> K{是否发生实质性变更?\n(忽略空白和仅注释变更)}
    K -->|否| L[不生成变更警告]
    K -->|是| M[为该函数生成 problem_type=4 的问题记录]
    I --> N[合并其他注释缺失/语法错误等问题]
    L --> N
    M --> N
    N --> O[将所有问题写入数据库\n并在前端展示问题列表]
```

---

### 3. 检查范围

- 仅在「项目检查（全量检查）」功能中启用本特性；单文件检查不进行 Git 变更分析。
- 仅对扩展名为 `.c` 的文件进行函数变更检测（`HEAD~1` 与当前工作区均以 `.c` 为过滤条件）。
- 匿名函数不在检查范围内（沿用现有解析规则）。

---

### 4. 「实质性变更」的定义

对比当前工作区与 `HEAD~1` 中、同一文件路径 + 同一函数签名所对应的函数体内容时：

- 忽略以下情况：
  - 仅有空格、缩进、换行等空白字符的变化；
  - 仅有函数体内部注释内容的变化（包括行注释与块注释），业务代码未变化。
- 只要在忽略空白和注释之后，函数体文本与 `HEAD~1` 中对应函数体**不完全相同**，即视为发生了「实质性变更」。

一旦发生实质性变更，即使注释已经被用户手动更新，也仍然会生成一条「内容变更警告」，用于提示该函数近期发生过实现变更。

---

### 5. Git 使用约定与前置条件

- 扩展允许在后台直接调用系统中的 `git` 命令（例如 `git rev-parse`, `git diff`, `git show` 等）来获取历史版本内容。
- 前置条件：
  - 当前工作区根目录（即运行项目检查的根目录）能够找到 `.git` 目录；
  - 当前分支存在至少两条提交，保证 `HEAD~1` 可用。
- 如果任一前置条件不满足：
  - 在 VS Code 输出区打印提示信息：`"当前项目未使用 git，已跳过变更检测"`；
  - 不执行任何 Git 相关逻辑，不生成 problem_type=4 的问题；
  - 其他注释缺失、语法错误等检查仍按原有逻辑正常执行。
- 如果系统中未安装 git 或调用 git 命令失败：
  - 视同「未使用 git」场景处理：输出一条错误/提示信息并跳过变更检测，整个检查流程不中断。

---

### 6. 新增/删除函数的处理规则

- 新增函数：
  - 在 `HEAD~1` 中不存在、但当前工作区中出现的新函数，视为「新增函数」。
  - 对于新增函数**不生成** problem_type=4 的内容变更警告。
  - 该函数仍然会参与注释缺失等其它问题类型的检测。
- 删除函数：
  - 在 `HEAD~1` 中存在、但当前工作区已删除的函数，不产生任何问题记录。
  - 不需要为删除函数记录任何「变更警告」。

---

### 7. 问题记录存储规范（problem_type=4）

当某个既有函数被判定为发生「实质性变更」时，需要向 SQLite 数据库的 `problems` 表插入一条问题记录，约定如下：

- `problem_type`：固定为 `4`，表示「内容变更警告」。
- `file_path`：函数所在文件的相对路径（与现有问题类型保持一致）。
- `function_name`：函数名。
- `function_signature`：函数签名，用于与历史版本函数唯一对应。
- `line_number` / `column_number`：指向当前版本函数定义所在行/列，用于前端跳转。
- `problem_description`：固定文案：
  - `"函数体相对于上一次提交发生变更，请检查注释是否同步更新。"`
- `function_snippet`：保存**当前版本**的函数代码片段（非老版本），用于前端展示。
- `check_timestamp` / `status`：遵循现有问题记录的写入规则，不做特殊扩展：
  - `check_timestamp` 使用本次检查时间；
  - `status` 默认 `0`（正常），可被前端后续标记为「已完成」。

前端展示与筛选方面：
- 变更警告作为一种普通问题类型出现在问题列表中；
- 现有「按问题类型筛选」功能中应包含 problem_type=4（内容变更警告）。

---

### 8. 性能策略

为避免在大型仓库中做无谓的历史读取，采用以下性能策略：

- 在执行项目检查前，通过 `git diff HEAD~1` 获取与上一条提交相比发生变更的文件列表；
- 仅对 diff 结果中扩展名为 `.c` 的文件执行「函数体比对 + 变更警告」逻辑；
- 不在 diff 列表中的 `.c` 文件视为「函数实现未发生变更」，不做 Git 层面的函数体比较，也不会生成 problem_type=4 的问题；
- 如果 diff 中仅包含非 `.c` 文件，则本次检查不会产生任何变更警告，但不会影响注释缺失和语法错误等其它类型的检查。

---

### 9. 首次提交与历史不足的处理

- 当当前分支只有一条提交，或无法解析 `HEAD~1`（例如刚初始化仓库、尚未完成第二次提交）时：
  - 视为「历史不足」，本次检查不进行 Git 变更检测；
  - 不为任何函数生成内容变更警告；
  - 仅执行原有注释检查和语法检查逻辑。
- 也就是说：
  - 「内容变更警告」仅从第二次提交开始才有意义；
  - 首次有 commit 时，所有函数虽然都是「新内容」，但不报变更警告。

---

### 10. 配置项：是否启用 Git 变更警告

新增一个设置项，用于控制该功能是否启用：

- 配置键名：`doc-doctor.enableGitChangeWarning`
- 类型：`boolean`
- 默认值：`true`（默认开启）

行为约定：

- 当 `doc-doctor.enableGitChangeWarning = true` 时：
  - 项目检查会在执行文件解析与注释检查之前，尝试进行 Git 环境检测和 `HEAD~1` 对比；
  - 成功时会按上述规则生成 problem_type=4 的问题。
- 当 `doc-doctor.enableGitChangeWarning = false` 时：
  - 完全不执行任何 Git 相关逻辑；
  - 不生成任何内容变更警告问题；
  - 其余检查逻辑保持不变。

---

### 11. 功能验收标准

- 在启用 `doc-doctor.enableGitChangeWarning` 且当前仓库存在至少两条提交的前提下：
  - 当某个 `.c` 文件中某函数的函数体实现（忽略空白和注释）相对于 `HEAD~1` 发生变更时：
    - 数据库中应新增一条 problem_type=4 的问题记录，且 `problem_description` 文案准确；
    - 前端展示区能够正确展示该问题，并可按问题类型筛选出内容变更警告。
  - 当函数只调整了空格、缩进、换行或仅修改了函数体内部注释时：
    - 不应生成任何 problem_type=4 的问题。
  - 当新增一个函数时：
    - 不生成 problem_type=4 的问题，但若注释缺失仍会生成相应注释缺失问题。
- 当当前工作区不是 Git 仓库、历史提交不足两条，或系统中找不到 git 命令时：
  - 在输出区提示：`"当前项目未使用 git，已跳过变更检测"`（或包含类似含义的错误信息）；
  - 整体检查过程不中断，注释缺失和语法错误等其它问题类型仍能正常识别并展示。

---

## 功能二：AI 辅助更新函数注释

### 1. 功能概述

在现有问题列表基础上，为支持类型为「注释缺失」或「内容变更警告」的问题增加一个「AI 修复注释」入口。当用户点击该入口时，扩展会基于当前函数源码、已有注释、问题类型以及必要的 Git 变更信息，调用外部 AI 服务生成或更新 Doxygen 注释，并在用户确认后将生成的注释写回源码文件。

该功能目标：

- 减少手动编写/维护 Doxygen 注释的工作量；
- 确保注释内容与当前函数实现保持一致；
- 保持注释风格统一（Doxygen 规范，简洁、职责明确）。

---

### 2. 适用问题类型

仅对以下问题类型开放 AI 修复能力：

- `problem_type = 1`：参数说明缺失；
- `problem_type = 2`：返回值说明缺失；
- `problem_type = 3`：函数功能说明（`@brief`）缺失；
- `problem_type = 4`：内容变更警告；

不对 `problem_type = 5`（语法错误）开放 AI 修复按钮，因为此时函数无法被可靠解析。

---

### 3. 前端交互设计

#### 3.1 按钮位置与交互

- 在问题列表中，每一条问题卡片上增加一个小图标或按钮，例如「AI 修复注释」。
- 仅当该问题的 `problem_type` 属于 1/2/3/4 且全局 AI 功能开关开启时，此按钮处于可点击状态；否则按钮隐藏或禁用。
- 用户点击按钮后：
  - 立即在该问题卡片上显示加载状态（例如旋转图标或“AI 处理中……”提示）；
  - 禁用该问题上的再次点击，直到本次请求完成（成功或失败）。

#### 3.2 结果预览与应用

- AI 返回结果后，不立即修改源文件，而是：
  - 打开一个预览界面（可为普通文本编辑器或 diff 视图）；
  - 左侧显示原始注释（若无则为空）；右侧显示 AI 生成的新注释；
  - 用户可在预览界面中点击「应用修改」按钮，将新注释写回对应源码文件；
  - 用户也可以关闭预览或点击「取消」，不对源码做任何改动。

---

### 4. AI 调用与配置

#### 4.1 配置来源

- AI 相关参数从 VS Code `settings.json` 中读取，主要包括：
  - `doc-doctor.ai.endpoint`：AI 服务 HTTP 接口地址；
  - `doc-doctor.ai.apiKey`：AI 服务访问密钥；
  - 其他可选参数（如模型名称、超时时间等）可视需要在后续扩展。
- API Key 等敏感信息不写死在代码中，必须通过设置配置。
- 在插件的设置界面中，需要对上述配置项给出清晰说明与输入框提示。

#### 4.2 全局开关

- 新增设置项：
  - `doc-doctor.enableAIRefactor: boolean`，表示是否启用 AI 修复注释功能；
  - 默认值建议为 `false` 或由产品决策；本需求仅说明需要该开关。
- 行为约定：
  - 当 `doc-doctor.enableAIRefactor = false` 时：
    - 前端不展示「AI 修复注释」按钮；
    - 后端不发起任何 AI 请求。
  - 当 `doc-doctor.enableAIRefactor = true` 且 `endpoint/apiKey` 未正确配置时：
    - 点击按钮后直接给出错误提示（例如“AI 服务未配置完整，请先在设置中填写接口地址和密钥”），不发起网络请求。

---

### 5. 上下文打包规则

向 AI 服务发起请求时，需要构造请求体，包含以下关键信息：

- 函数完整源码（包含函数头和函数体）；
- 当前已有注释内容（如果有），包括 Doxygen 注释块或其它注释；
- 问题类型（1=参数缺失，2=返回值缺失，3=函数体说明缺失，4=内容变更警告）；
- 文件路径、函数名、函数签名等元信息；
- 若为 problem_type=4（内容变更警告）：
  - 附带最近一次 Git 变更的摘要信息（例如该函数相关的 diff 片段或说明“本次修改点”），以便 AI 更好理解逻辑变化。

不需要发送整文件源码，只发送当前函数及少量必要上下文，以降低隐私和流量成本。

---

### 6. 生成注释的规则

#### 6.1 语言与风格

- 注释语言遵循「跟随当前注释语言」的原则：
  - 若函数已有注释且主要为英文，则生成英文注释；
  - 若无明显英文注释，则默认使用简体中文注释。
- 注释必须使用 Doxygen 风格，至少包含：
  - `@brief`：简洁的函数职责说明；
  - `@param`：对每个参数进行说明；
  - `@return`：对返回值进行说明（仅对非 `void` 函数）。
- 要求整体风格简洁，重点描述职责和输入输出，不进行过度推理或长篇解释。

#### 6.2 不同问题类型下的生成策略

- 对 `problem_type = 1/2/3`（注释缺失）：
  - AI 仅生成/补全 Doxygen 注释块（`/** ... */`），不修改任何业务代码；
  - 注释块插入位置为函数定义正上方；
  - 若该函数已有部分注释，则以现有注释为基础进行补全和重写，尽量保持原有有用信息。
- 对 `problem_type = 4`（内容变更警告）：
  - AI 在原注释基础上进行「最小必要改动」，只增删修改与本次逻辑变更相关的描述；
  - 不要求保留原格式中的多余内容，但应尽量避免大幅度风格迁移；
  - 最终结果仍需是一个完整的 Doxygen 注释块。

#### 6.3 插件侧格式校验与轻量修正

- 插件在接收到 AI 生成的注释后，应做一次轻量级格式校验：
  - 确认是否存在 `@brief` / `@param` / `@return`（对于非 void 函数）标签；
  - 若缺失关键标签，可由插件自动补一个空壳条目（例如补一个 `@return` 行，内容可为“返回值说明待补充”），以避免后续检查直接报错；
  - 其余复杂的语义验证仍交由后续正常检查流程处理。

---

### 7. 隐私与文档说明

- 用户需明确知情：启用 AI 修复功能时，插件会将部分源码（函数实现与注释内容）发送到配置的 AI 服务；
- 需要在 README 和设置界面中增加明显的说明文字，例如：
  - “本功能会将当前函数的源代码和注释内容发送到你配置的 AI 服务，用于生成或更新注释。”
- 插件不记录、不上传 AI 返回结果以外的额外用户数据。

---

### 8. 错误与异常处理

- 在调用 AI 接口过程中，可能出现以下异常：
  - 网络超时；
  - DNS/连接错误；
  - 鉴权失败（API Key 无效或权限不足）；
  - AI 返回内容格式不符合预期（缺少需要的字段等）。
- 出现异常时，应在前端给出相对详细的错误提示，至少包含：
  - 错误类型（网络错误 / 鉴权失败 / 服务错误 / 解析错误等）；
  - 简要错误信息，便于用户排查配置问题。
- 对于某个问题的单次 AI 调用失败：
  - 结束 loading 状态，恢复按钮可点击；
  - 不对源码做任何改动。

---

### 9. 并发与费用控制

- 对同一条问题记录：
  - 在一次 AI 请求未完成前，禁止重复点击同一问题上的「AI 修复注释」按钮；
  - 请求完成后（成功或失败）才恢复按钮状态。
- 本需求不强制增加二次确认弹窗（用户点击即发起调用），但需通过设置项 `doc-doctor.enableAIRefactor` 由用户显式启用该能力，以降低误触和费用风险。

---

### 10. 写回源码与后续检查

- 当用户在预览界面点击「应用修改」后：
  - 插件将 AI 生成的注释插入或替换到对应源码文件中；
  - 利用 VS Code 自身的撤销机制（Ctrl+Z）支持用户回滚。
- 写回规则：
  - 对注释缺失类问题（1/2/3）：在函数定义上方插入一整块新的 Doxygen 注释，或以新注释整体替换原来不规范的注释；
  - 对内容变更警告（4）：优先以整体替换的方式，用新的 Doxygen 注释块覆盖原有注释，确保最终结构规范。
- 写回完成后：
  - 插件本身不自动触发项目检查或单文件检查；
  - 由用户根据需要手动点击「项目检查」或「单文件检查」按钮，以刷新问题列表状态。

---

### 11. 功能验收标准

- 在正确配置 `doc-doctor.ai.endpoint`、`doc-doctor.ai.apiKey` 且启用 `doc-doctor.enableAIRefactor` 的前提下：
  - 对任意 problem_type 为 1/2/3/4 的问题：
    - 问题卡片中应展示「AI 修复注释」按钮；
    - 点击后能看到 loading 状态，并在请求结束后进入预览界面；
    - 预览界面的新注释满足 Doxygen 基本格式要求；
    - 用户点击「应用修改」后，源码文件中对应函数上方的注释被正确插入或替换。
  - 对 problem_type=5（语法错误）的问题：
    - 问题卡片中不出现「AI 修复注释」按钮。
- 在 AI 接口配置不完整或调用失败时：
  - 前端能给出明确错误提示；
  - 源码文件不会被修改；
  - 问题卡片按钮状态在请求结束后恢复可点击。
- 在未启用 `doc-doctor.enableAIRefactor` 时：
  - 问题列表中不出现任何与 AI 相关的按钮；
  - 不发起任何对外的 AI 网络请求。
